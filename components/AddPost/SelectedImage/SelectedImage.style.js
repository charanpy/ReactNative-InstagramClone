import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  Image: {
    width: '100%',
    height: '100%',
    resizeMode: 'cover',
  },
  Selected_Image: {
    marginTop: '5%',
    width: '100%',
    height: '50%',
    overflow: 'scroll',
  },
});

export default styles;
